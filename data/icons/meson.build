icondir = join_paths(get_option('datadir'), 'icons/hicolor')

install_data(
  'io.github.lainsce.Notejot.svg',
  install_dir: join_paths(icondir, 'scalable/apps'),
)
install_data(
  'io.github.lainsce.Notejot.Devel.svg',
  install_dir: join_paths(icondir, 'scalable/apps'),
)