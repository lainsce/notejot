# Notejot app tests

This directory is reserved for app-level Xcode tests. The independent
`NotejotCore` package keeps its unit tests in `lib/Tests` so the
library remains self-contained and testable outside the Notejot app.
